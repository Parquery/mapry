{
  "name": "Some_graph",
  "description": "defines some object graph.",
  "cpp": {
    "namespace": "some::graph",
    "path_as": "boost::filesystem::path",
    "optional_as": "boost::optional",
    "datetime_library": "ctime"
  },
  "go": {
    "package": "somegraph"
  },
  "py": {
    "module_name": "some.graph",
    "path_as": "pathlib.Path",
    "timezone_as": "str"
  },
  "properties": {
    "some_map": {
      "type": "map",
      "description": "defines some map.",
      "values": {
        "type": "integer",
        "minimum": 0
      }
    },
    "some_nested_map": {
      "type": "map",
      "description": "defines some nested map.",
      "values": {
        "type": "map",
        "values": {
          "type": "integer",
          "minimum": 0
        }
      }
    }
  }
}
