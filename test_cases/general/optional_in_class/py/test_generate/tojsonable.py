# File automatically generated by mapry. DO NOT EDIT OR APPEND!


"""serializes to JSONable objects."""


import collections
import typing

import some.graph


def serialize_with_optional(
        instance: some.graph.WithOptional,
        ordered: bool = False
) -> typing.MutableMapping[str, typing.Any]:
    """
    serializes an instance of WithOptional to a JSONable representation.

    :param instance: the instance of WithOptional to be serialized
    :param ordered:
        If set, represents the instance as a ``collections.OrderedDict``.
        Otherwise, it is represented as a ``dict``.
    :return: a JSONable
    """
    if ordered:
        target = (
            collections.OrderedDict()
        )  # type: typing.MutableMapping[str, typing.Any]
    else:
        target = dict()

    ##
    # Serialize some_text
    ##

    if instance.some_text is not None:
        target['some_text'] = instance.some_text

    return target


def serialize_some_graph(
        instance: some.graph.SomeGraph,
        ordered: bool = False
) -> typing.MutableMapping[str, typing.Any]:
    """
    serializes an instance of SomeGraph to a JSONable.

    :param instance: the instance of SomeGraph to be serialized
    :param ordered:
        If set, represents the instance properties and class registries
        as a ``collections.OrderedDict``.
        Otherwise, they are represented as a ``dict``.
    :return: JSONable representation
    """
    if ordered:
        target = (
            collections.OrderedDict()
        )  # type: typing.MutableMapping[str, typing.Any]
    else:
        target = dict()

    ##
    # Serialize instance registry of WithOptional
    ##

    if len(instance.with_optionals) > 0:
        if ordered:
            target_with_optionals = (
                collections.OrderedDict()
            )  # type: typing.MutableMapping[str, typing.Any]
        else:
            target_with_optionals = dict()

        for id, with_optional_instance in instance.with_optionals.items():
            if id != with_optional_instance.id:
                raise ValueError(
                    'Expected ID {!r} of the instance of WithOptional, but got: {!r}'.format(
                        id, with_optional_instance.id))

            target_with_optionals[id] = serialize_with_optional(
                instance=with_optional_instance,
                ordered=ordered)
        target['with_optionals'] = target_with_optionals

    return target


# File automatically generated by mapry. DO NOT EDIT OR APPEND!
