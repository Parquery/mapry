// File automatically generated by mapry. DO NOT EDIT OR APPEND!

#include "jsoncpp.h"
#include "parse.h"
#include "types.h"

#include <cstring>
#include <memory>
#include <sstream>
#include <stdexcept>
#include <string>
#include <utility>

namespace some {
namespace graph {

namespace jsoncpp {

/**
 * generates an error message.
 *
 * @param cc char array as the description part of the message
 * @param cc_size size of the char array
 * @param s string as the detail part of the message
 * @return concatenated string
 */
std::string message(const char* cc, size_t cc_size, std::string s) {
  std::string result;
  result.reserve(cc_size + s.size());
  result.append(cc, cc_size);
  result.append(s);
  return result;
}

/**
 * converts a JSON value type to a human-readable string representation.
 *
 * @param value_type to be converted
 * @return string representation of the JSON value type
 */
std::string value_type_to_string(Json::ValueType value_type) {
  switch (value_type) {
    case Json::ValueType::nullValue: return "null";
    case Json::ValueType::intValue: return "int";
    case Json::ValueType::uintValue: return "uint";
    case Json::ValueType::realValue: return "real";
    case Json::ValueType::stringValue: return "string";
    case Json::ValueType::booleanValue: return "bool";
    case Json::ValueType::arrayValue: return "array";
    case Json::ValueType::objectValue: return "object";
    default:
      std::stringstream ss;
      ss << "Unhandled value type in value_to_string: "
        << value_type;
      throw std::domain_error(ss.str());
  }
}

void some_graph_from(
    const Json::Value& value,
    std::string ref,
    SomeGraph* target,
    parse::Errors* errors) {
  if (errors == nullptr) {
    throw std::invalid_argument("Unexpected null errors");
  }

  if (not errors->empty()) {
    throw std::invalid_argument("Unexpected non-empty errors");
  }

  if (not value.isObject()) {
    constexpr auto expected_but_got(
      "Expected an object, but got: ");

    errors->add(
      ref,
      message(
        expected_but_got,
        strlen(expected_but_got),
        value_type_to_string(
          value.type())));
    return;
  }

  ////
  // Parse some_datetime
  ////

  if (not value.isMember("some_datetime")) {
    errors->add(
      ref,
      "Property is missing: some_datetime");
  } else {
    const Json::Value& value_0 = value["some_datetime"];
    if (not value_0.isString()) {
      constexpr auto expected_but_got(
        "Expected a string, but got: ");

      errors->add(
        std::string(ref)
          .append("/some_datetime"),
        message(
          expected_but_got,
          strlen(expected_but_got),
          value_type_to_string(
            value_0.type())));
    } else {
      const std::string cast_0 = value_0.asString();
      struct tm tm_0 = tm{0};
      char* ret_0 = strptime(
        cast_0.c_str(),
        "%Y/%m/%d %H-%M-%SZ",
        &tm_0);

      if (ret_0 == nullptr or *ret_0 != '\0') {
        constexpr auto expected_but_got(
          "Expected to strptime "
          "%Y/%m/%d %H-%M-%SZ"
          ", but got: ");

        errors->add(
          std::string(ref)
            .append("/some_datetime"),
          message(
            expected_but_got,
            strlen(expected_but_got),
            cast_0));
      } else {
        target->some_datetime = tm_0;
      }
    }
  }
  if (errors->full()) {
    return;
  }

  ////
  // Parse formatless_datetime
  ////

  if (not value.isMember("formatless_datetime")) {
    errors->add(
      ref,
      "Property is missing: formatless_datetime");
  } else {
    const Json::Value& value_1 = value["formatless_datetime"];
    if (not value_1.isString()) {
      constexpr auto expected_but_got(
        "Expected a string, but got: ");

      errors->add(
        std::string(ref)
          .append("/formatless_datetime"),
        message(
          expected_but_got,
          strlen(expected_but_got),
          value_type_to_string(
            value_1.type())));
    } else {
      const std::string cast_1 = value_1.asString();
      struct tm tm_1 = tm{0};
      char* ret_1 = strptime(
        cast_1.c_str(),
        "%Y-%m-%dT%H:%M:%SZ",
        &tm_1);

      if (ret_1 == nullptr or *ret_1 != '\0') {
        constexpr auto expected_but_got(
          "Expected to strptime "
          "%Y-%m-%dT%H:%M:%SZ"
          ", but got: ");

        errors->add(
          std::string(ref)
            .append("/formatless_datetime"),
          message(
            expected_but_got,
            strlen(expected_but_got),
            cast_1));
      } else {
        target->formatless_datetime = tm_1;
      }
    }
  }
  if (errors->full()) {
    return;
  }
}

/**
 * serializes the date/time/datetime to a string.
 *
 * @param[in] t time structure
 * @param[in] fmt format
 * @return time structure serialized to a string according to the format
 */
std::string tm_to_string(const struct tm& t, const char* fmt) {{
  if(fmt == nullptr or fmt[0] == '\0') {
    return "";
  }

  const size_t fmt_size = strlen(fmt);

  std::string buf;
  buf.resize(fmt_size * 4);
  int len = strftime(&buf[0], buf.size(), fmt, &t);

  while(len == 0) {{
    buf.resize(buf.size() * 2);
    int len = strftime(&buf[0], buf.size(), fmt, &t);
  }}
  buf.resize(len);
  return buf;
}}

Json::Value serialize_some_graph(
    const SomeGraph& some_graph) {
  Json::Value some_graph_as_value;

  some_graph_as_value["some_datetime"] = tm_to_string(
    some_graph.some_datetime,
    "%Y/%m/%d %H-%M-%SZ");

  some_graph_as_value["formatless_datetime"] = tm_to_string(
    some_graph.formatless_datetime,
    "%Y-%m-%dT%H:%M:%SZ");

  return some_graph_as_value;
}

}  // namespace jsoncpp

}  // namespace graph
}  // namespace some

// File automatically generated by mapry. DO NOT EDIT OR APPEND!
