# File automatically generated by mapry. DO NOT EDIT OR APPEND!


"""parses JSONable objects."""


import typing

import some.graph
import some.graph.parse


def some_graph_from(
        value: typing.Any,
        ref: str,
        errors: some.graph.parse.Errors
) -> typing.Optional[some.graph.SomeGraph]:
    """
    parses SomeGraph from a JSONable value.

    :param value: JSONable value
    :param ref: reference to the value (e.g., a reference path)
    :param errors: errors encountered during parsing
    :return: parsed SomeGraph, or None if ``errors``
    """
    if errors.full():
        return None

    if not isinstance(value, dict):
        errors.add(
            ref,
            "Expected a dictionary, but got: {}".format(type(value)))
        return None

    graph = some.graph.parse.placeholder_some_graph()

    ##
    # Parse some_float_gt_0_lt_100
    ##

    value_0 = value.get(
        'some_float_gt_0_lt_100',
        None)

    if value_0 is None:
        errors.add(
            ref,
            'Property is missing: some_float_gt_0_lt_100')
    else:
        if not isinstance(value_0, (int, float)):
            errors.add(
                '/'.join((
                    ref, 'some_float_gt_0_lt_100')),
                'Expected a number, but got: {}'.format(
                    type(value_0)))
        else:
            if not (value_0 > 0):
                errors.add(
                    '/'.join((
                        ref, 'some_float_gt_0_lt_100')),
                    'Expected > 0, but got: {}'.format(
                        value_0))
            if not (value_0 < 100):
                errors.add(
                    '/'.join((
                        ref, 'some_float_gt_0_lt_100')),
                    'Expected < 100, but got: {}'.format(
                        value_0))
            else:
                graph.some_float_gt_0_lt_100 = float(value_0)

    if errors.full():
        return None

    ##
    # Parse some_float_ge_0_le_100
    ##

    value_2 = value.get(
        'some_float_ge_0_le_100',
        None)

    if value_2 is None:
        errors.add(
            ref,
            'Property is missing: some_float_ge_0_le_100')
    else:
        if not isinstance(value_2, (int, float)):
            errors.add(
                '/'.join((
                    ref, 'some_float_ge_0_le_100')),
                'Expected a number, but got: {}'.format(
                    type(value_2)))
        else:
            if not (value_2 >= 0):
                errors.add(
                    '/'.join((
                        ref, 'some_float_ge_0_le_100')),
                    'Expected >= 0, but got: {}'.format(
                        value_2))
            if not (value_2 <= 100):
                errors.add(
                    '/'.join((
                        ref, 'some_float_ge_0_le_100')),
                    'Expected <= 100, but got: {}'.format(
                        value_2))
            else:
                graph.some_float_ge_0_le_100 = float(value_2)

    if errors.full():
        return None

    ##
    # Parse unconstrained_float
    ##

    value_4 = value.get(
        'unconstrained_float',
        None)

    if value_4 is None:
        errors.add(
            ref,
            'Property is missing: unconstrained_float')
    else:
        if not isinstance(value_4, (int, float)):
            errors.add(
                '/'.join((
                    ref, 'unconstrained_float')),
                'Expected a number, but got: {}'.format(
                    type(value_4)))
        else:
            graph.unconstrained_float = float(value_4)

    if errors.full():
        return None

    if not errors.empty():
        return None

    return graph
